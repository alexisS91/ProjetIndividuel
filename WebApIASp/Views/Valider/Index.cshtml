@model IEnumerable<WebApIASp.Models.Valider>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_valider)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Commentaire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_projet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_utilisateur)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_valider)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Commentaire)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_projet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_utilisateur)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.id_valider}) |
            @Html.ActionLink("Details", "Details", new { id=item.id_valider }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_valider })
        </td>
    </tr>
}

</table>
